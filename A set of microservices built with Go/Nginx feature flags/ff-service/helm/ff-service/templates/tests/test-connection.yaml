apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ff-service.fullname" . }}-test-connection"
  labels:
    {{- include "ff-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ff-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
